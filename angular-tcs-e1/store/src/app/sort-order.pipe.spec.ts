import { SortOrderPipe } from './sort-order.pipe';

describe('SortOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
